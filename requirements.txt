# Local/desktop runtime dependencies for YouTube Downloader (Kivy + KivyMD + yt-dlp).
# Install FFmpeg separately so merged video+audio downloads work:
#   macOS: brew install ffmpeg
#   Ubuntu: sudo apt install ffmpeg

kivy
kivymd
yt-dlp
buildozer